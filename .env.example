# Server-only configuration. Copy to .env locally; never commit real secrets.
PORT=3000
RP_NAME=DialysisSafe
RP_ID=localhost
ORIGIN=http://localhost:3000
# Put third-party API keys here only if the server uses them:
# SOME_API_KEY=replace-with-local-secret
